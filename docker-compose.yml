version: '3'

services:
  api:
    build: .
    ports:
      - 80:80
    command: npm start
    container_name: tackleAI
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge  